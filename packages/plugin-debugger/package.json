{
  "name": "@itly/plugin-debugger",
  "version": "0.0.0",
  "description": "Debugger Plugin",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "yarn run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "itly",
    "sdk",
    "iteratively",
    "plugin"
  ],
  "author": "Iteratively",
  "license": "ISC",
  "devDependencies": {
    "@itly/sdk": "^0.4.0",
    "typescript": "^3.9.3"
  },
  "dependencies": {
  },
  "peerDependencies": {
    "@itly/sdk": "^0.4.0"
  }
}
