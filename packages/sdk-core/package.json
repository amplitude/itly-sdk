{
  "name": "@itly/sdk-core",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "LICENSE",
    "CHANGELOG.md",
    "README.md",
    "build-info.json",
    "dist/",
    "package.json"
  ],
  "scripts": {
    "prepublish": "yarn run build",
    "prepublishOnly": "yarn run build && node ./prepublish-setup && cp -r ./dist/* . && rm -rf ./dist",
    "postpublish": "git clean -fd",
    "build": "tsc",
    "clean": "rimraf dist/*",
    "test": "jest --runInBand"
  },
  "keywords": [
    "itly",
    "sdk",
    "iteratively"
  ],
  "author": "Iteratively",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.0",
    "jest": "^26.0.1",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.0",
    "typescript": "^3.9.3"
  },
  "gitHead": "6694fa97c4d96177674b6acc491b19c3013387c3"
}
